header
  nav
    a(class="logo" href="/") Blogger
    if user
      a(class="account" href="/profile")
        img(src=user.gravatar(50), alt="")
        p= user.name
    else
      ul(clss="login")
        a(class="login-link nav-link" href="/login") Login
        a(class="signup-link signup-link-mobile nav-link" href="/signup") Sign up